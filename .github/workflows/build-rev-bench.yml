name: build-rev-bench

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/workflows/build-rev-bench.yml', 'rev/**']

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Extract headers + source
        run: |
          tar xzf rev/rev_bench.tar.gz -C /tmp
          ls /tmp/tflite_neuron_bench.cc /tmp/hdrs/flatbuffers/flatbuffers.h 2>/dev/null

      - name: Install NDK r21e (clang 12 compile) + r27 (new lld link)
        run: |
          curl -sL -o /tmp/ndk.zip "https://dl.google.com/android/repository/android-ndk-r21e-linux-x86_64.zip"
          cd /tmp && unzip -q ndk.zip
          /tmp/android-ndk-r21e/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android21-clang++ --version | head -1
          curl -sL -o /tmp/ndk27.zip "https://dl.google.com/android/repository/android-ndk-r27-linux.zip"
          cd /tmp && unzip -q ndk27.zip
          /tmp/android-ndk-r27/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android24-clang++ --version | head -1

      - name: Compile + link with r27 (unified libc++ ABI __1)
        run: |
          CXX27=/tmp/android-ndk-r27/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android24-clang++
          cp rev/libtflite_mtk.so /tmp/
          # r27 clang + TF2.8 headers; force libc++ ABI namespace __1 (device TFLite uses this)
          $CXX27 -std=c++17 /tmp/tflite_neuron_bench.cc \
            -I /tmp/tf28_src/tensorflow-2.8.0 -I /tmp/hdrs \
            -D_LIBCPP_ABI_NAMESPACE=__1 \
            -L/tmp -l:libtflite_mtk.so -llog \
            -Wl,-rpath,/data/local/tmp \
            -o /tmp/tflite_neuron_bench 2>&1 | head -15
          echo "BUILD=$?"
          ls -la /tmp/tflite_neuron_bench 2>/dev/null && file /tmp/tflite_neuron_bench

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: rev-bench-binary
          path: /tmp/tflite_neuron_bench
          if-no-files-found: warn
